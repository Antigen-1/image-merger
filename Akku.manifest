#!r6rs ; -*- mode: scheme; coding: utf-8 -*-
(import (akku format manifest))

(akku-package ("image-merger" "1.0.0-alpha.0")
  (synopsis "Stitch images into a grid according to an s-exp config")
  (authors "Guy Q. Schemer")
  (license "MIT")
  (depends ("chez-srfi" ">=0.0.0-akku <1.0.0"))
)
